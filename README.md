# Mauritius Mass Finder v13.6

Public-facing bilingual live-updating build.

Upload all files in this folder to GitHub Pages. Normal schedule updates can be made by editing `data/masses.json` or by configuring a Google Sheets CSV feed in `config.js`.
